import { DragObjectDirective } from './drag-object.directive';

describe('DragObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new DragObjectDirective();
    expect(directive).toBeTruthy();
  });
});
